id: rule-ks-off-switches
status: current
scope: work-bundle
applies_to: {paths: [skills/ks-*/SKILL.md, references/ks-*], skills: [ks-*], artifacts: []}
enable_when: [user disables persistence or asks draft only]
severity: must
rule: ks-off-switches applies to directive-scoped migrated skills.
required_behavior: [stop durable writes and report waiting state when off-switches apply]
prohibited_behavior: [persist despite explicit pause, draft-only, or stop instruction]
validation: [required fields exist, generated skills cite applicable rule]
source_authority: [ks SKILL off-switches]
deprecated_sources: []
