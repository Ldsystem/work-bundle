id: rule-ks-perspective-routing
status: current
scope: work-bundle
applies_to: {paths: [skills/ks-*/SKILL.md, references/ks-*], skills: [ks-*], artifacts: []}
enable_when: [note path or perspective is selected]
severity: must
rule: ks-perspective-routing applies to directive-scoped migrated skills.
required_behavior: [use the most specific lifecycle-aware leaf perspective]
prohibited_behavior: [store durable facts in broad containers or wrong implementation-only homes]
validation: [required fields exist, generated skills cite applicable rule]
source_authority: [ks perspectives and workflow]
deprecated_sources: []
